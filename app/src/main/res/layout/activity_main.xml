<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.firs.facedetecttosvr.MainActivity">



    <Button
        android:id="@+id/BluetoothButton"
        android:layout_height="50dp"
        android:layout_width="150dp"
        android:onClick="startBluetoothActivity"
        android:text="蓝牙模式"
        >


        </Button>

    <Button
        android:id="@+id/CaptureButton"
        android:layout_toEndOf="@+id/BluetoothButton"
        android:text="拍摄模式"
        android:onClick="startCaptureActivity"
        android:layout_height="50dp"
        android:layout_width="150dp">


    </Button>

    <Button
        android:id="@+id/ImageButton"
        android:layout_below="@+id/BluetoothButton"
        android:text="图片模式"
        android:onClick="startImageActivity"
        android:layout_height="50dp"
        android:layout_width="150dp">


    </Button>
    <Button
        android:id="@+id/RealTimeButton"
        android:layout_toEndOf="@+id/ImageButton"
        android:layout_below="@+id/BluetoothButton"
        android:onClick="startRealTimeActivity"
        android:text="实时实名模式"
        android:layout_height="50dp"
        android:layout_width="150dp">


    </Button>
</RelativeLayout>
